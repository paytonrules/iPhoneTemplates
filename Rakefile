$: << require File.expand_path(File.dirname(__FILE__) + "/")

task :default => [:copy_files]

task :copy_files do
  # Go through each project in Project Templates
  # Create a google toolbox directory - if it doesn't exist
  # Copy all the files in UnitTestingFiles/Google Toolbox overwriting
  # The same with frameworks
  # Create a Unit Test Target - if one does not exist.  Add all .m files to it
  # Add the OCMock framwework to it
  # Setup the dependency
end